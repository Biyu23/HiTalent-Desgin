## zh-CN

默认 `iconPosition`为`left`, 可以设置四个方向`left`|`right`|`top`|`bottom`的图标位置。

## en-US

The `iconPosition` defaults to `left`, but can be set to any of the four directions: `left`, `right`, `top`, or `bottom`.
